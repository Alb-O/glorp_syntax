[package]
name = "liney-queries"
description = "Reusable structural query helpers"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
liney-syntax-tree = { path = "../syntax-tree" }
ropey.workspace = true

[dev-dependencies]
tree-sitter-rust.workspace = true
